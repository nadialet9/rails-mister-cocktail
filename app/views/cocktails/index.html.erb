

<div class="container">

<h1 class="text-center mb-9"> Wich cocktail do you want to try ? </h1>

  <%= link_to("Create", new_cocktail_path) %>

  <div class="row d-flex flex-wrap justify-content-around">

      <% @cocktails.each do |cocktail| %>
        <div class="card col-3 pb-4 m-2" style="height: 400px; background-color: rgba(0,0,255,0.1);" >
          <img class="card-img-top" src="..." alt="Card image cap">
          <div class="card-body">
            <h5 class="card-title"><%= link_to(cocktail.name, cocktail_path(cocktail)) %></h5>
          </div>

          <a class="btn btn-primary" href="<%= cocktail_path(cocktail) %>"> Go to Cocktail 🍸 </a>
        </div>
      <% end %>
    </div>

</div>
